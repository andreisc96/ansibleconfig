[Unit]
Description=Node Exporter
Wants=network-online.target
After=network-online.target

[Service]
User=node_exporter
ExecStart=/usr/local/bin/{{ exporter }} --web.listen-address=:{{ exporter_port }}
Restart=always

[Install]
WantedBy=multi-user.target